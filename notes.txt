## for saving python script in package.json
    "build": {
        "extraResources": "python_scripts",
    }

## example 1 running python

    npm install --save python-shell 

    var pyshell =  require('python-shell');

    pyshell.run('hello.py',  function  (err, results)  {
    if  (err)  throw err;
    console.log('hello.py finished.');
    console.log('results', results);
    });

## example 2 running python
    var python = require('child_process').spawn('python', ['./hello.py']);
        python.stdout.on('data',function(data){
            console.log("data: ",data.toString('utf8'));
        });

## npm run dist (installer)
## npm run pack (folder)

## learn to open new window on button press in html.